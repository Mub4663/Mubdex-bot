services:
  - type: worker
    name: mubdex-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mubdex_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false   # you set this in Render dashboard
